kind = "web"
previewPath = "/"
title = "AI Proxy Portal"
version = "1.0.0"
id = "artifacts/ai-proxy-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19286

[services.development]
run = "pnpm --filter @workspace/ai-proxy-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ai-proxy-portal", "run", "build" ]
publicDir = "artifacts/ai-proxy-portal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19286"
BASE_PATH = "/"
